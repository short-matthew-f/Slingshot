var Universe = function () {
  this.planets = {};
  this.ships   = {};
  this.holes   = {};
};

Universe.prototype.add = function (body) {
  switch (body.type) {
    case expression:

      break;
    default:

  }
}
